package com.christycatlin.try1.bank;

public interface IAccount {
    //create account (apply for account as customer, employee does account approval)
    //login (different implementations for customer and employee)
}
